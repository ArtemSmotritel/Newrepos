<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <script type="text/javascript">
    var arr = [1,6,7,-5,12,0,-4,10]


    var min = arr.reduce(function(a, b) {
      return Math.min(a, b);
    });
    var max = arr.reduce(function(a, b) {
      return Math.max(a, b)
    });
    var sum = arr.reduce(function(a, b) {
      return a + b; }, 0);
  var arr2 = [max, min, sum]
    var test = [1,3,4,5,2,0];
    // var rv = {};
    // function toObject(test) {
    //   for (var i = 0; i < test.length; i++) {
    //     rv[i] = arr[i];
    //   }
    //   return rv;
    // } // пытался преобразить в объект
    // console.log(rv);
    </script>
  </body>
</html>
